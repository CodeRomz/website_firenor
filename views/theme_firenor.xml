<odoo>
    <template id="theme_firenor_colors" inherit_id="website.assets_frontend" name="Firenor Theme Color Presets">
        <xpath expr="." position="inside">
            <!-- Define color presets for the theme editor -->
            <t t-set="o_color_palettes" t-value="dict(
                firenor = {
                    'preset-1': {'background': '#333333', 'foreground': '#ffffff'},  // Dark Gray with White
                    'preset-2': {'background': '#47a5ae', 'foreground': '#ffffff'},  // Teal with White
                    'preset-3': {'background': '#47a5ae', 'foreground': '#ffffff'},  // Teal (Accent) with White
                }
            )"/>
        </xpath>
    </template>
</odoo>
